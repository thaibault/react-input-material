--- ../node_modules/@rmwc/ripple/index.mjs	2023-12-01 11:10:17
+++ index.alternate.mjs	2023-12-01 17:22:40
@@ -96,6 +96,7 @@
     t.disabled && n.handleBlur();
   }, [t.disabled, n]), { ...i };
 }, H = y.createContext({});
+var mergeRefs = function(a,b) { return function(el) { if (typeof a === 'function') a(el); else if (a && 'current' in a) a.current = el;if (typeof b === 'function') b(el); else if (b && 'current' in b) b.current = el}};
 function j(t) {
   const {
     children: l,
@@ -126,8 +127,9 @@
     "mdc-ripple-upgraded--background-focused",
     ""
   ));
+
   const E = y.cloneElement(c, {
-    ref: d.reactRef,
+    ref: mergeRefs(c.ref, d.reactRef),
     ...c.props,
     ...b,
     ...d.props({
